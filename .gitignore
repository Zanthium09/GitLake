# Secrets — never commit
.env
*.pem
credentials.json

# Raw + intermediate data. GitHub Archive is ~10 GB for the target week;
# a single `git add -A` here would poison history permanently.
data/
spark-warehouse/
*.json.gz

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Spark / Delta local artifacts
metastore_db/
derby.log
checkpoints/

# dbt
dbt_project/target/
dbt_project/dbt_packages/
dbt_project/logs/
dbt_project/profiles.yml

# Airflow (Astro)
airflow/logs/
airflow_settings.yaml
.astro/

# OS / editor
.DS_Store
.idea/
.vscode/
